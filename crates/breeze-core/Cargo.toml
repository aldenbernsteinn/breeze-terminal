[package]
license = "GPL-3.0-or-later"
name = "breeze-core"
version = "0.1.0"
edition = "2021"
description = "Breeze platform-agnostic core: session state machine, throttle policy, config, transcript, split tree"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
breeze-vt = { path = "../breeze-vt" }
